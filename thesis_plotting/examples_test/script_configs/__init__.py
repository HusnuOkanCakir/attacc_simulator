"""Stub `script_configs` package — minimal subset of symbols that the
reference plotting scripts at /home/okan/plotting_examples/ import.

This exists ONLY so we can run one reference script with fabricated
data, to visually anchor our thesis_plotting/style/ choices.

Do NOT depend on this stub from anywhere outside examples_test/.
"""
